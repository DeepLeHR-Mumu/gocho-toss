{
  "$schema": "https://turborepo.org/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".next/**"]
    },
    "e2e": { "outputs": [] },
    "e2e:CICD": { "outputs": [] }
  },
  "globalEnv": ["NEXT_PUBLIC_VERCEL_ENV", "NODE_ENV"]
}
