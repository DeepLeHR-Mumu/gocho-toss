import { css } from "@emotion/react";

import { COLORS } from "shared-style/color";

export const cssObj = {
  msg: css`
    font-size: 0.875rem;
    font-weight: 500;
    color: ${COLORS.ERROR_RED40};
  `,
};
