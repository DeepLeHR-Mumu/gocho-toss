import { css } from "@emotion/react";

import { COLOR } from "shared-style/color";

export const cssObj = {
  msg: css`
    font-size: 0.875rem;
    font-weight: 500;
    color: ${COLOR.RED200};
    line-height: 2;
  `,
};
