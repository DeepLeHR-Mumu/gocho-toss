import { css } from "@emotion/react";
import { subPageTitle } from "@/style/commonStyles";

export const cssObj = {
  wrapper: css``,
  title: css`
    ${subPageTitle};
  `,
  container: css`
    margin-bottom: 3rem;
  `,
};
