export interface setCarouselSettingDef {
  (): {
    dots: boolean;
    arrows: boolean;
    infinite: boolean;
    autoplaySpeed: number;
    swipeToSlide: boolean;
    slidesToShow: number;
    speed: number;
  };
}
