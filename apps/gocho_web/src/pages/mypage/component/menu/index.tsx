import { FunctionComponent } from "react";
import { tempCSS } from "./style";

export const Menu: FunctionComponent = () => {
  return (
    <section>
      <div css={tempCSS} />
      <div css={tempCSS} />
    </section>
  );
};
