import { css } from "@emotion/react";

import { COLORS } from "shared-style/color";

export const cssObj = {
  cardContainer: css`
    margin-top: 2rem;
    padding: 1.5rem;
    border: 1px solid ${COLORS.GRAY80};
  `,
};
